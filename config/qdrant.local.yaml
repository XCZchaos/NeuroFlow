log_level: INFO
telemetry_disabled: true

storage:
  # 使用相对于项目根目录的路径，克隆到其他电脑后无需修改盘符。
  storage_path: "./.runtime/qdrant/storage"
  snapshots_path: "./.runtime/qdrant/snapshots"

service:
  host: 127.0.0.1
  http_port: 6333
  grpc_port: 6334
  enable_cors: false

cluster:
  enabled: false
